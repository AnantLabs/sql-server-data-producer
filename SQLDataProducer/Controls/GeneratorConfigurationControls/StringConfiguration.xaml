<UserControl x:Class="SQLRepeater.Controls.GeneratorConfigurationControls.StringConfiguration"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="25" />
            <RowDefinition Height="25" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50*" />
            <ColumnDefinition Width="50*" />
        </Grid.ColumnDefinitions>


        <TextBlock Grid.Row="0" Text="Min Length:" />
        <TextBlock Grid.Row="1" Text="Max Length:" />

        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding MinLength}" />
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding MaxLength}" />

    </Grid>
</UserControl>
