<UserControl x:Class="SQLRepeater.Views.ExecutionDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:me="clr-namespace:SQLRepeater.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50*" />
            <ColumnDefinition Width="50*" />
        </Grid.ColumnDefinitions>
        
        <DataGrid 
            Grid.Column="0"
            ItemsSource="{Binding Path=SelectedExecutionItem.TargetTable.Columns}" AutoGenerateColumns="True"
            SelectedItem="{Binding SelectedColumn, Mode=TwoWay}" Grid.ColumnSpan="2">
                <!--<DataGrid.Columns>
                    <DataGridTextColumn Binding="{Binding ColumnName}" Header="Column Name" />
                    <DataGridTextColumn Binding="{Binding ColumnDataType}" Header="Data Type" />
                   
                    <DataGridTemplateColumn Header="Data Generator">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <ComboBox ItemsSource="{Binding DataGenerationGenerators}"
                                          SelectedItem="{Binding CurrentColumnEntity.ColumnValue.ValueGenerator, Mode=TwoWay}"
                                          Height="25" VerticalAlignment="Top">
                                        <ComboBox.ItemTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding Method.Name}" />
                                            </DataTemplate>
                                        </ComboBox.ItemTemplate>

                                    </ComboBox>
                            </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    
                </DataGrid.Columns>-->
        </DataGrid>
        <DataGrid 
            Grid.Column="1"
            ItemsSource="{Binding Path=SelectedColumn.Generator.GeneratorParameters}" AutoGenerateColumns="True"
            SelectedItem="{Binding SelectedGeneratorParameter, Mode=TwoWay}" Grid.ColumnSpan="2">
            <!--<DataGrid.Columns>
                    <DataGridTextColumn Binding="{Binding ColumnName}" Header="Column Name" />
                    <DataGridTextColumn Binding="{Binding ColumnDataType}" Header="Data Type" />
                   
                    <DataGridTemplateColumn Header="Data Generator">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <ComboBox ItemsSource="{Binding DataGenerationGenerators}"
                                          SelectedItem="{Binding CurrentColumnEntity.ColumnValue.ValueGenerator, Mode=TwoWay}"
                                          Height="25" VerticalAlignment="Top">
                                        <ComboBox.ItemTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding Method.Name}" />
                                            </DataTemplate>
                                        </ComboBox.ItemTemplate>

                                    </ComboBox>
                            </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    
                </DataGrid.Columns>-->
        </DataGrid>
    </Grid>
</UserControl>
